[pytest]
testpaths = src/backend/tests
pythonpath = src/backend
addopts = -ra -q --basetemp=.pytest_tmp
# Note: --basetemp redirects tmp_path away from the system TEMP
# (pytest-of-<user> dir has a broken ACL on this machine).
